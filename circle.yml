machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t pascalgrimaud/jboss-as:7.1.1 .
test:
  override:
    - docker run -d -p 8084:8080 -p 9994:9990 --name=jboss -e JBOSS_PASS="pgrimaud" pascalgrimaud/jboss-as:7.1.1 ; sleep 10
    - docker logs jboss
